import BuildingInProgress from "@/components/BuildingInProgress";

export default function NotFound() {
  return (
    <BuildingInProgress statusText="error 404: seems like we lost each other" />
  );
}
